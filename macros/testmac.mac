; a macro is a group of OMA commands contained in a text file;; use a ";" for comments;; first open a data file called jet.datget jet; display itdisp;; you can do loops inside of macros; loop from n = 20 to 200 in steps of 20loop n 20 200 20	; create a noise image with mean 0 and rms given by n	noise 0 @n	; add the test image	add jet	; display the result	disp	; end the looploopnd;; running this macro once will display 11 different images